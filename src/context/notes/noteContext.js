import { createContext } from "react";

const noteContext = createContext(); // just asking react to create a new context named noteContext

export default noteContext;

// no needed a separate file to write this code -> just for clarity and simplicity